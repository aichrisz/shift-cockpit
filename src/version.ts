/** App version — keep in sync with package.json. */
export const APP_VERSION = '0.9.0'

/** i18n keys for What’s new bullets (v0.9). */
export const WHATS_NEW_KEYS = [
  'whatsNew1',
  'whatsNew2',
  'whatsNew3',
  'whatsNew4',
  'whatsNew5',
] as const
